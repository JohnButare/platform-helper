#!/bin/bash
. $BIN/function.sh

echo "Starting applications..."
pushd "$BIN/win32" >/dev/nul ; start "/Program Files/AutoHotkey/AutoHotkey.exe" $(utw "$HOME/Documents/data/bin/keys.ahk"); popd >/dev/nul
tc startup.btm

[[ $BASH_DEBUG ]] && pause
IsInteractive && return 0 || exit 0
