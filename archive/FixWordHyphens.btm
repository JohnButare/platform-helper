@echo off

REM For all file names in the current dir, replace word hypens (char 8211) with a standard hyphen.
REM When creating video filenames and cut and pasting from Word, a non-standard
REM hyphen (ascii 8211) may be put into the filename.  The describe command
REM does not work with this special hyphen (it fails siltently to add the comment).

for %f in (*) (
		
	iff %@index["%f",%@char[8211]] != -1 then
		echos .
		move /q "%f" "%@replace[%@char[8211],-,%f]"
	endiff

)