@echo off
SetLocal
set program=%@search[Defraggler.exe]

iff %@IsElevated[] == 1 then
	REM Defraggler does not start in the background
	start /pgm "%program"
else
	REM Defraggler requires elevation and does not always prompt for it
	call sudo.btm "%program"
endiff