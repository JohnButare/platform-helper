@echo off
SetLocal

iff %# != 2 .or. not IsDir %@UnQuote[%1] then 
	EchoErr `usage: HideFiles <directory> <files to hide>`
	quit 1
endiff

set dir=%@UnQuote[%1]
set FilesToHide=%@UnQuote[%2]

pushd %@quote[%dir] >& nul:
if %? != 0 quit %?

attrib /e /s/a:-h  +h %FilesToHide
quit %?
