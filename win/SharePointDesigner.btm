@echo off

REM Initialize
call office.btm init

set program=%OfficeDir\SpDesign.exe

REM Start the program
iff exist "%program" then
	start /pgm "%program" %$
else
	call TextEdit.btm %$
endiff
