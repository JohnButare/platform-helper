@echo off

set FullName=%@word[2,%@execstr[net user %username |& egrep -i "Full Name"]]

REM Return the username if the full name could not be determined (if in a domain).
iff "%FullName" == "" then
  echo %UserName
else
  echo %FullName  
endiff
