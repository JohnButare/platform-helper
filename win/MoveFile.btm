@echo off

REM Arguments
if %@IsHelpArg[%@UnQuote[%1]] == 1 goto usage

if %# != 2 goto usage

if exist %@quote[%1] move /q %@quote[%1] %@quote[%2]
quit %?

:usage
text 1>&2
usage: MoveFile <src> <dest>
endtext
quit 1
