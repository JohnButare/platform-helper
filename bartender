#!/usr/bin/env bash
. app.sh || exit

usage()
{
	ScriptUsage "$1" "\
Usage: $(ScriptName) [OPTION]... [start|close|profile|program|IsInstalled](start)
Bartender commands."
}

init()
{
	profileName="bartender"
	program="$P/Bartender 4.app"
	profileDir="$HOME/Library/Preferences"
	profileMask="com.surteesstudios.Bartender.plist"
}

IsInstalledCommand() { [[ -d "$program" ]]; }
profileUsage() { echot "Usage: $(ScriptName) profile dir|SaveDir|save|restore [<profile name>|default](latest)\n$(ScriptName) configuration."; }
profileArgStart() { profileArgs=(); }
profileArgs() { profileArgs=( "$@" ); (( shift+=$# )); }
profileCommand() { profile $noPrompt --app "$profileName" --method "$profileDir" --files "$profileMask" "${profileArgs[@]}"; }
startCommand() { IsInstalledCommand && open -a "$(GetFileNameWithoutExtension "$program")" "$@"; }

ScriptRun "$@"
