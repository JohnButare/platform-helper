#!/bin/bash
. bash.bashrc
. function.sh

! IsElevated && { EchoErr "bootstrap must be run as administrator"; pause; exit 1; }

ask 'Synchronize files' && SyncLocalFiles --src-older "$1"

inst bootstrap
pause
